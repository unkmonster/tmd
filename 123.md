## Repository 层缺失功能详细分析

### 一、已实现的文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `interfaces.go` | ✅ 已定义 | 接口定义完整 |
| `user_repo.go` | ✅ 已实现 | 用户仓库实现 |
| `list_repo.go` | ✅ 已实现 | 列表仓库实现 |
| `entity_repo.go` | ✅ 已实现 | 实体仓库实现 |
| `link_repo.go` | ✅ 已实现 | 链接仓库实现 |
| `previous_name_repo.go` | ❌ **缺失** | 接口定义但未实现 |
| `query.go` | ❌ **缺失** | 查询功能完全缺失 |
| `user_sync.go` | ❌ **缺失** | 用户同步功能缺失 |

---

### 二、详细缺失功能

#### 1. PreviousNameRepository 实现缺失

**interfaces.go 中定义的接口** (第 109-113 行):
```go
type PreviousNameRepository interface {
	GetByUserID(ctx context.Context, userID uint64, limit, offset int) ([]*UserPreviousName, error)
	Create(ctx context.Context, name *UserPreviousName) error
}
```

**稳定版对应功能** (`database/query.go:129-136`):
```go
func QueryUserPreviousNames(db *sqlx.DB, uid uint64, limit, offset int) ([]UserPreviousName, error) {
	var names []UserPreviousName
	err := db.Select(&names,
		"SELECT * FROM user_previous_names WHERE uid = ? ORDER BY record_date DESC LIMIT ? OFFSET ?",
		uid, limit, offset)
	return names, err
}
```

**缺失影响**: 无法分页查询用户历史名称记录

---

#### 2. Query 查询功能完全缺失

**稳定版 query.go 中的功能** (`database/query.go`):

| 函数名 | 功能描述 | 代码行 |
|--------|----------|--------|
| `Count` | 通用计数查询 | 19-34 |
| `BuildSearchCondition` | 构建搜索条件 | 36-50 |
| `QueryUsers` | 分页查询用户 | 52-66 |
| `QueryLists` | 分页查询列表 | 68-82 |
| `QueryUserEntities` | 分页查询用户实体 | 84-98 |
| `QueryLstEntities` | 分页查询列表实体 | 100-114 |
| `QueryUserLinks` | 查询用户链接 | 116-127 |
| `QueryUserPreviousNames` | 查询用户历史名称 | 129-136 |

**缺失影响**: API 层的分页查询功能无法正常工作

---

#### 3. User Sync 用户同步功能缺失

**稳定版 user_sync.go** (`database/user_sync.go:7-41`):
```go
func SyncUser(db *sqlx.DB, userId uint64, name string, screenName string, 
              isProtected bool, friendsCount int, accessible bool) error {
	renamed := false
	isNew := false
	usrdb, err := GetUserById(db, userId)
	if err != nil {
		return err
	}

	if usrdb == nil {
		isNew = true
		usrdb = &User{}
		usrdb.Id = userId
	} else {
		renamed = usrdb.Name != name || usrdb.ScreenName != screenName
	}

	usrdb.FriendsCount = friendsCount
	usrdb.IsProtected = isProtected
	usrdb.Name = name
	usrdb.ScreenName = screenName
	usrdb.IsAccessible = accessible

	if isNew {
		err = CreateUser(db, usrdb)
	} else {
		err = UpdateUser(db, usrdb)
	}
	if err != nil {
		return err
	}
	if renamed || isNew {
		return RecordUserPreviousName(db, userId, name, screenName)
	}
	return nil
}
```

**功能说明**:
- 根据用户ID查询用户是否存在
- 如果不存在则创建新用户
- 如果存在则检查用户名/显示名是否变更
- 如果变更则记录历史名称
- 更新用户信息

**缺失影响**: 用户同步逻辑需要手动实现，代码重复

---

#### 4. UserRepository 中缺失的方法

**稳定版有但重构版缺失**:

| 方法名 | 稳定版位置 | 功能描述 |
|--------|-----------|----------|
| `SetUsersAccessible` | `user.go:69-86` | 批量设置用户可访问状态 |
| `MarkListMembersAccessibleByIDs` | `user.go:88-98` | 标记列表成员可访问（异步） |

**稳定版实现**:
```go
func SetUsersAccessible(db *sqlx.DB, uids []uint64) error {
	if len(uids) == 0 {
		return nil
	}
	query := `UPDATE users SET is_accessible=1 WHERE id IN (?)`
	query, args, err := sqlx.In(query, uids)
	if err != nil {
		return fmt.Errorf("failed to build batch update query: %w", err)
	}
	query = db.Rebind(query)
	_, err = db.Exec(query, args...)
	if err != nil {
		return fmt.Errorf("failed to batch update users accessible status: %w", err)
	}
	return nil
}

func MarkListMembersAccessibleByIDs(db *sqlx.DB, uids []uint64) {
	if len(uids) == 0 || db == nil {
		return
	}
	go func() {
		if err := SetUsersAccessible(db, uids); err != nil {
			log.Debugln("failed to mark list members as accessible:", err)
		}
	}()
}
```

---

### 三、缺失功能使用场景

| 缺失功能 | 使用场景 | 影响程度 |
|----------|----------|----------|
| `previous_name_repo.go` | API 查询用户历史名称 | 🔴 高 |
| `query.go` | API 分页查询用户/列表/实体 | 🔴 高 |
| `user_sync.go` | 用户数据同步（创建/更新） | 🟡 中 |
| `SetUsersAccessible` | 批量标记用户可访问 | 🟡 中 |
| `MarkListMembersAccessibleByIDs` | 列表下载后标记成员 | 🟡 中 |

---

### 四、建议修复方案

#### 高优先级（必须修复）

1. **创建 `previous_name_repo.go`**
2. **创建 `query.go`** - 或者将查询方法添加到各 Repository 接口

#### 中优先级（建议修复）

3. **创建 `user_sync.go`** - 或在 UserRepository 中添加 `Sync` 方法
4. **在 UserRepository 中添加批量操作方法**

需要我帮你实现这些缺失的功能吗？